[project]
name = "chess-trainer-backend"
version = "0.1.0"
description = "AI Chess Coach API"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "python-chess",
    "google-genai",
    "requests"
]

[tool.vercel]
entrypoint = "backend.main:app"
